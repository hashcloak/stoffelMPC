use types::numbers::secret_sharing::SecretSharing;
use types::numbers::{
    MPCType,
    fixed::{PubFixed, SecFixed},
    gf2::{PubGf2, SecGf2},
    int::{PubInt, SecInt},
};

use super::processors::processor::{Processor};
use super::state::{Memory, Register, StackRegister};

struct StoffelVM {
   
}

impl StoffelVM {

}