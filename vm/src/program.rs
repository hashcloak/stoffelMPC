use super::instructions::opcodes::Opcodes;

pub struct Program(Vec<Opcodes>);

impl Program {
    fn new() -> Self {
        todo!();
    }

    fn parse() {
        todo!();
    }

    fn execute() {
        todo!();
    }
}