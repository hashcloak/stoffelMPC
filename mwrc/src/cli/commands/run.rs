use clap::Parser;

/// Runs a StoffelMPC project with the given parameters
#[derive(Debug, Default, Parser)]
pub struct Command {

}

pub(crate) fn exec(command: Command) -> Result<()> {
    todo!();
}